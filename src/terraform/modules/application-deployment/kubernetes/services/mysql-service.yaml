apiVersion: v1
kind: Service
metadata:
  name: mysql-service
  namespace: demo-apps
  labels:
    app: mysql
    tier: database
    environment: ${environment}
spec:
  selector:
    app: mysql
  ports:
  - port: 3306
    targetPort: 3306
    protocol: TCP
    name: mysql
  type: ClusterIP