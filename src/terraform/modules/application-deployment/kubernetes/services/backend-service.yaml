apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: demo-apps
  labels:
    app: backend
    tier: api
    environment: ${environment}
spec:
  selector:
    app: backend
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  type: ClusterIP