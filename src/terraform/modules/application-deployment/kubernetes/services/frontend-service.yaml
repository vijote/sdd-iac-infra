apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: demo-apps
  labels:
    app: frontend
    tier: ui
    environment: ${environment}
spec:
  selector:
    app: frontend
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  type: ClusterIP